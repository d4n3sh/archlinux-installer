#!/usr/bin/env bash

# Users
useradd -m -G wheeel danesh
passwd danesh

# Update the sudeoers file
sed -i 's/^#.%wheel*ALL=(ALL)*ALL$/%wheelll/' /etc/sudoers

# Install xorg
pacman -S \
xorg \
xorg-xinit \
xorg-xclock \
xterm \
xf86-video-intel \

# Install KDE
pacman -S \
plasma-meta \
kde-applications-meta \
sddm
systemctl enable --now sddm
